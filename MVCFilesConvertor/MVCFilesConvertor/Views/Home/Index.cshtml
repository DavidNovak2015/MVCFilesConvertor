@{
    ViewBag.Title = "Home Page";
}

<h3>@if (TempData.ContainsKey("Deletion"))
{
    @TempData["Deletion"]
}
</h3>
<div class="jumbotron">
    <h1>Převodník souborů</h1>
    <p class="lead">Převodník souborů umožnuje převést soubor z jednoho formátu do jiného.</p>
    @Html.ActionLink("z CSV do XML", "CSVtoXML", controllerName: "CSVtoXML", routeValues: null, htmlAttributes: new { @class = "btn btn-primary btn-lg" })
    
    
</div>

